# 🚀 دليل التشغيل والنشر الشامل لتطبيق Shego and Hego

**المؤلف**: Manus AI
**التاريخ**: 24 يناير 2026
**الإصدار**: 1.0.0

هذا الدليل يوضح لك خطوة بخطوة كيفية نشر التطبيق على جميع المنصات (Google Play، App Store، والويب) والبدء في جني الأرباح.

## المرحلة الأولى: التحضير والإعداد الأساسي

قبل البدء في النشر، تأكد من توفر المتطلبات التالية:

1. **حساب Google Play Developer**: قم بزيارة `https://play.google.com/console` وأنشئ حساباً جديداً (رسوم التسجيل: 25 دولار).
2. **حساب Apple Developer**: قم بزيارة `https://developer.apple.com` وأنشئ حساباً (رسوم التسجيل: 99 دولار سنوياً).
3. **حساب Firebase**: قم بزيارة `https://firebase.google.com` وأنشئ مشروعاً جديداً باسم "shego-and-hego-app".
4. **حساب Stripe**: قم بزيارة `https://stripe.com` وأنشئ حساباً تجارياً لمعالجة الدفع.
5. **حساب AdMob**: قم بزيارة `https://admob.google.com` وأنشئ تطبيقاً جديداً لعرض الإعلانات.

## المرحلة الثانية: بناء التطبيق

### الخطوة 1: تثبيت المتطلبات

افتح الطرفية (Terminal) وقم بتنفيذ الأوامر التالية:

```bash
cd /home/ubuntu/shego-and-hego
npm install
npm install -g eas-cli
```

### الخطوة 2: تحديث ملف `.env`

قم بإنشاء ملف `.env` وأضف البيانات التالية:

```env
FIREBASE_API_KEY=your_firebase_api_key
FIREBASE_PROJECT_ID=shego-and-hego-app
STRIPE_PUBLISHABLE_KEY=pk_live_your_stripe_key
ADMOB_BANNER_ID=ca-app-pub-xxxxxxxxxxxxxxxx~yyyyyyyyyy
ADMOB_REWARDED_ID=ca-app-pub-xxxxxxxxxxxxxxxx~yyyyyyyyyy
```

### الخطوة 3: بناء التطبيق للويب

```bash
npm run web
```

هذا سيشغل التطبيق محلياً على `http://localhost:8081`.

## المرحلة الثالثة: النشر على Google Play

### الخطوة 1: إنشاء ملف التوقيع (Keystore)

```bash
keytool -genkey -v -keystore my-release-key.keystore -keyalg RSA -keysize 2048 -validity 10000 -alias my-key-alias
```

### الخطوة 2: بناء ملف APK

```bash
eas build --platform android --type apk
```

### الخطوة 3: رفع على Google Play

1. اذهب إلى `https://play.google.com/console`.
2. انقر على "إنشاء تطبيق جديد".
3. أدخل اسم التطبيق: "Shego and Hego".
4. حمّل ملف APK الذي قمت ببناؤه.
5. أضف وصفاً للتطبيق يحتوي على الكلمات المفتاحية.
6. انقر على "نشر" (Publish).

**الرابط النهائي**: `https://play.google.com/store/apps/details?id=com.shegoandhego.app`

## المرحلة الرابعة: النشر على App Store (iOS)

### الخطوة 1: بناء ملف IPA

```bash
eas build --platform ios --type app-store
```

### الخطوة 2: رفع على App Store

1. اذهب إلى `https://appstoreconnect.apple.com`.
2. انقر على "تطبيقاتي" (My Apps).
3. انقر على "تطبيق جديد" (New App).
4. أدخل البيانات الأساسية.
5. حمّل ملف IPA الذي قمت ببناؤه.
6. انقر على "إرسال للمراجعة" (Submit for Review).

**ملاحظة**: قد تستغرق عملية المراجعة من 24 إلى 48 ساعة.

## المرحلة الخامسة: رفع صفحة الهبوط على الويب

### الخطوة 1: اختر مضيف ويب

يمكنك استخدام أحد الخيارات التالية:

| المضيف | السعر | الميزات |
| :--- | :--- | :--- |
| **Vercel** | مجاني | سرعة عالية، CDN عالمي |
| **Netlify** | مجاني | سهل الاستخدام، نشر تلقائي |
| **AWS S3** | رخيص | تخزين موثوق، CDN قوي |

### الخطوة 2: رفع الملفات

1. قم بتحميل ملف `landing-page.html` من مجلد المشروع.
2. اربط الدومين الخاص بك (مثلاً: `www.shegoandhego.com`).
3. فعّل شهادة SSL (HTTPS) لضمان الأمان.

## المرحلة السادسة: تفعيل نظام الأرباح

### الخطوة 1: ربط Stripe

1. اذهب إلى `https://dashboard.stripe.com`.
2. انسخ مفاتيح API الخاصة بك.
3. أضفها في ملف `.env`.

### الخطوة 2: تفعيل AdMob

1. اذهب إلى `https://admob.google.com`.
2. أنشئ تطبيقاً جديداً باسم "Shego and Hego".
3. انسخ معرفات الإعلانات (Ad Unit IDs).
4. أضفها في ملف `.env`.

## المرحلة السابعة: المراقبة والتحسين

### تتبع الأداء

استخدم Google Analytics لتتبع سلوك المستخدمين:

```bash
npm install firebase-analytics
```

### تحديث التطبيق

لتحديث التطبيق بميزات جديدة:

```bash
npm run build
eas submit --platform android
eas submit --platform ios
```

---

**تهانينا!** لقد قمت بنشر تطبيق "Shego and Hego" بنجاح على جميع المنصات. ابدأ الآن في جني الأرباح والتوسع عالمياً!
